[build]
  # Static site deployment - no build needed
  command = "echo 'Static site ready'"
  publish = "out"
  
[build.environment]
  NODE_VERSION = "18"

# Disable Next.js plugin for static sites
[build.processing]
  skip_processing = false

# Headers for API endpoints
[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "Content-Type"
    Cache-Control = "public, max-age=30, must-revalidate"  # 30 saniye cache

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "ALLOWALL"
    Content-Security-Policy = "frame-ancestors 'self' https://arena.social https://*.arena.social;"
    Access-Control-Allow-Origin = "https://arena.social"
    Access-Control-Allow-Credentials = "true"
    Access-Control-Allow-Methods = "GET, POST, OPTIONS, PUT, DELETE"
    Access-Control-Allow-Headers = "Content-Type, Authorization, X-Requested-With"

# API endpoint redirects
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200

[[redirects]]
  from = "/health"
  to = "/api/index.json"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
